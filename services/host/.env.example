# Claude API-sleutel
ANTHROPIC_API_KEY=sk-ant-...

# Claude-modus
CLAUDE_MODEL=claude-sonnet-4-6

# VLAM-modus (UbiOps/Mistral)
VLAM_API_KEY=...
VLAM_BASE_URL=https://api...
VLAM_MODEL_ID=ubiops-deployment/...


# MCP-servers (pad naar server-scripts)
MCP_SERVER_KVK=../mcp/kvk/server.py
MCP_SERVER_KOOP=../mcp/koop/server.py
MCP_SERVER_REGELRECHT=../mcp/regelrecht/server.py
MCP_SERVER_RVO=../mcp/rvo/server.py
# De Business Wallet (netbeheerder-mock) staat standaard AAN. Alleen voor een
# onderzoek op eigen opgaven van de ondernemer: MCP_SERVER_NETBEHEERDER=uit.
# Zet dat nooit op een gedeelde omgeving; de host waarschuwt bij het opstarten
# en /health toont het onder `bronnen_uit`.
MCP_SERVER_NETBEHEERDER=../mcp/netbeheerder/server.py

# BAG API Individuele Bevragingen (Kadaster) voor live woonfunctie/gebruiksdoel.
# Gratis API-key aanvragen bij Kadaster (sinds 1-3-2023 verplicht een eigen key).
# Zonder key gebruikt de KvK-server demo-data voor de bekende demo-adressen.
# BAG_API_KEY=...

# RegelRecht engine (poc-machine-law) JSON-RPC endpoint. De code-default wijst naar
# de actuele demo-host; override hier als die verhuist.
# REGELRECHT_RPC_URL=https://ui.lac.projects.digilab.network/mcp/rpc

# Toegestane KvK-nummers voor de gesloten testgroep (MVP-01/PDR-009). De frontend
# stuurt `X-Test-User: <kvk-nummer>` van de gekozen persona; staat het nummer niet
# in deze lijst, dan blokkeert de host met "log eerst in" en raadpleegt geen bron.
# Geen geheim (de nummers staan publiek in de frontend), wel een grens.
# De vier profielen hebben mockdata in de KvK- en netbeheerder-server:
#   85234567 = Koffiezaak Noon      (persona koffiezaak)     — plicht via elektriciteit
#   62345681 = Kwekerij De Bloesem  (persona bloemenkweker)  — plicht via gas en elektriciteit
#   56789012 = Roots & Locks        (persona haarstylist)    — geen plicht
#   61234570 = Vogel Bouwregie B.V. (persona bouwmanagement) — plicht via elektriciteit
# Deze lijst moet elke persona dekken die de frontend op `actief` zet; staat er
# één niet in, dan ziet de respondent zijn bedrijf op het scherm en krijgt hij
# van de assistent "log eerst in". Bij een wijziging ook de component-config op
# ZAD bijwerken — die leest deze variabele uit de omgeving, niet uit dit bestand.
# TEST_KVK_NUMMERS=85234567,62345681,56789012,61234570

# Dev-fallback KvK-nummer voor de KvK-server bij STANDALONE gebruik (buiten de
# host, zonder sessie). NIET nodig als de host draait; die injecteert het
# sessie-KvK. Er is geen hardcoded demo-bedrijf meer.
# DEMO_KVK_NUMMER=85234567

# Host-instellingen
VLAM_HOST=0.0.0.0
VLAM_PORT=8000

# Grenzen per LLM-aanroep (seconden), op basis van meting (PDR-013): de zwaarste
# beurt kost 20 s op Claude; de grens draagt een staart van 3x plus één herkansing.
# Het getal komt terug in de foutmelding ("duurde langer dan 180 seconden").
# VLAM_TIMEOUT=120
# CLAUDE_TIMEOUT=180
# Bovengrens op het antwoord in tokens (langste gemeten: 1.400).
# LLM_MAX_TOKENS=2048
# Levensteken tijdens een lange aanroep (seconden tussen status-events) en de
# wachttijd vóór de ene herkansing als het model 'te druk' meldt.
# LLM_HARTSLAG_INTERVAL=10
# LLM_HERKANSING_WACHT=2
# LLM_HERKANSINGEN=2

# Time-out per bron-aanroep (seconden), voor beide transporten. Zonder deze grens
# kan een bron die het verzoek aanneemt maar nooit antwoordt de hele stream laten
# hangen. Het CLI-transport heeft daarbinnen een eigen grens: maximaal 30 s, en altijd
# 5 s korter dan deze.
# TOOL_TIMEOUT=45

# Maximale lengte van één vraag (tekens). Daarboven weigert de host meteen met
# een concrete melding, in plaats van de LLM-call verderop te laten falen.
# MAX_VRAAG_TEKENS=4000

# Security
# CORS: komma-gescheiden whitelist. LEEG is de strikte stand — dan is er geen
# enkele cross-origin toegang. Vul dit alleen in als de frontend écht op een
# andere origin draait dan de host, zoals hieronder bij lokale ontwikkeling.
# Zet hier niets in "voor de zekerheid": dat opent iets wat nu dicht is.
#
# Op de ZAD-deployment staat deze variabele daarom NIET gezet: de frontend praat
# daar via een same-origin reverse proxy, dus CORS komt er niet aan te pas
# (zie docs/deploy-zad.md). Moet er toch cross-origin verkeer bij, dan een
# concrete whitelist en nooit "*":
#   ALLOWED_ORIGINS=https://moza.overheid.nl,https://moza-test.overheid.nl
ALLOWED_ORIGINS=http://localhost:8080
# API-keys via request-headers accepteren (x-vlam-api-key / x-claude-api-key).
# Default: TRUE, en dat is een bewuste keuze — zie PDR-010. De deployment draait
# zonder LLM-sleutels; elke tester brengt zijn eigen sleutel mee via de UI. Zo is
# er geen gedeelde sleutel die leeggetrokken kan worden op een host die (nog)
# geen authenticatie op /chat heeft.
# Spelregels die daarbij horen:
#   - alleen over HTTPS, en met een dichte origin-grens (ALLOWED_ORIGINS leeg,
#     of anders een concrete whitelist — nooit "*");
#   - de frontend bewaart de sleutel in het browsergeheugen of sessionStorage,
#     nooit in localStorage en nooit in een URL.
# Zet op false zodra er wél server-sleutels staan (ANTHROPIC_API_KEY /
# VLAM_API_KEY hierboven); de header wordt dan genegeerd.
#
# Deze vlag bepaalt ook wélke melding de gebruiker ziet als er geen sleutel is:
# bij true "vul uw sleutel in bij Instellingen", bij false "meld dit bij de
# beheerder" — want dan negeert de host een ingevulde sleutel toch (PDR-011).
#
# Uitzondering: tijdens een begeleid gebruikersonderzoek staat deze op false én
# staat er wél een serversleutel, zodat deelnemers niets hoeven in te voeren.
ALLOW_API_KEY_OVERRIDE=true
